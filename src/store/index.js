import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export const mutations = {
}

export const actions = {
}

export const state = {
}

export default new Vuex.Store({
  mutations,
  actions,
  state
})
